# -*- coding: utf-8 -*-
"""Рубильники поставки.

Здесь лежат константы, которые включают и выключают функциональность на уровне
СБОРКИ, а не пользовательских настроек. Значение отсюда имеет ПРИОРИТЕТ над
config.json: выключенная здесь функция не включается ни из интерфейса, ни
правкой файла настроек.
"""

# US-086: REST API-сервер (FastAPI + uvicorn) ВЫКЛЮЧЕН в поставке по решению
# владельца продукта от 10.09.2026. Код api_server.py, страница swagger.html и
# поля api_* в config.py сохранены НАМЕРЕННО — ради будущего серверного режима
# (US-087: установка на корпоративный сервер, учёт по LDAP, обращения из
# телефонной системы). Зависимости fastapi/uvicorn/python-multipart убраны из
# requirements.txt и из сборки (build_exe.bat: --exclude-module), поэтому
# одного True здесь мало.
#
# Порядок возврата функции:
#   1. API_SERVER_ENABLED = True;
#   2. раскомментировать три зависимости в requirements.txt и установить их;
#   3. убрать --exclude-module fastapi/uvicorn/starlette/multipart/
#      python_multipart из build_exe.bat;
#   4. проверить api_server.py на совместимость с текущей версией FastAPI;
#   5. обновить tests/test_us086_api_server_off.py (проверки инвертируются);
#   6. пересобрать и убедиться, что порт снова слушается;
#   7. дописать в docs/enterprise-deploy.md, что появился слушающий сокет.
API_SERVER_ENABLED = False
