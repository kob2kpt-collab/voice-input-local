PySide6>=6.7
faster-whisper>=1.2.1
huggingface_hub>=0.24
sounddevice>=0.4.7
numpy>=1.26
pyperclip>=1.8.2
uiautomation>=2.0.20
onnx-asr[cpu,hub]>=0.11.0
onnxruntime!=1.24.1
pyinstaller>=6.0

soundfile>=0.12.1

av>=12.0

# Summarization (SUM-04): local LLM on CPU
# llama-cpp-python requires CMake + C++ compiler.
# Install separately: pip install llama-cpp-python
# On Windows without build tools, use pre-built wheel:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
# The app works without it — summarization will be unavailable.

# API server (API-01..04)
fastapi>=0.111
uvicorn>=0.29
python-multipart>=0.0.9

# Cloud STT (US-015, US-016, US-032): OpenAI-compatible API + ElevenLabs
requests>=2.31

# Экспорт расшифровок из истории в PDF (US-024)
fpdf2>=2.7
