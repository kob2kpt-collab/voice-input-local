.venv/
build/
dist/
release/*.exe
release/*.zip
version_info.txt
__pycache__/
*.pyc
*.pyo
*.log
launch_debug.log

# Local runtime data is stored in %LOCALAPPDATA%\VoiceInputLocal, not in the repo
config.json
history.json
*.sqlite
*.sqlite3
models/
logs/
recordings/

# Secrets
.env
*.token

# Internal project management (not for public release)
project-dashboard.html
project-dashboard.bat
project-data.json
CLAUDE.md
docs/testing/

# Backups created during AI-assisted edits (see CLAUDE.md, incident 2026-05-24)
*.bak

# Отчёты, собираемые к выпуску (US-089): в репозитории не хранятся,
# создаются скриптами и прикладываются к релизу как файлы выпуска.
sbom*.json
pip-audit*.json

# Локальные настройки сессии Claude Code
.claude/

# PyInstaller перегенерирует спек каждой сборкой; источник истины — build_exe.bat
VoiceInputLocal.spec
